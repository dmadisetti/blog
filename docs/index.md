---
# Landing is just the post list: no TOC, and no nav sidebar (the archive lives
# a tab away — it shouldn't share the front page with the posts themselves).
hide:
  - toc
  - navigation
---

# readme.<u>dm</u>

a small blog by [<u>@dm</u>adisetti](https://github.com/dmadisetti).
<p class="landing-sub" markdown="1">**vim** — `hjkl` or `←↓↑→` to move, `⏎` to open. The cursor unfolds what it lands on.</p>
