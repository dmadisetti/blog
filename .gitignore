# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
env/
ENV/
.venv

# MkDocs
site/
.cache/

# Node.js
node_modules/
npm-debug.log
yarn-error.log
package-lock.json
yarn.lock

# Build outputs
docs/javascripts/vim-navigation.js
docs/javascripts/vim-navigation.js.map

# Transient queue-preview mirrors (hooks/queue_preview.py) — never committed.
docs/posts/queued.*.md

# IDEs
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Nix
result
result-*

# Misc
.env
*.log
.claude/

# Private publishing queue — drafts, scheduled posts, and ideas.
# Intimate / unpublished work. NEVER committed to the public repo;
# posts only move into docs/blog/posts/ when they go live.
_queue/

# Old working backups + archive (contains personal journals + half-baked drafts).
# Local-only; pull keepers into _queue/ when triaged, don't publish wholesale.
backup/

# Personal reMarkable tablet dump (thesis, notebooks). The logo is inlined
# in overrides/partials/logo.html, so nothing here is needed by the build.
remarkable-pull/
# fetched/re-derivable map inputs (warmed cache lives in docs/setti/public/cache)
notebooks/data/*
!notebooks/data/sightings.csv
notebooks/__marimo__/

# osmnx Overpass HTTP cache — re-derivable
notebooks/cache/
